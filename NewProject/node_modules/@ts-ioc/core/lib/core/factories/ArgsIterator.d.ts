import { Metadate } from '../metadatas';
/**
 * arg checker.
 *
 * @export
 * @interface CheckExpress
 * @template T
 */
export interface CheckExpress<T extends Metadate> {
    /**
     * arg matched or not.
     *
     * @param {*} arg
     * @returns {boolean}
     * @memberof CheckExpress
     */
    match(arg: any): boolean;
    /**
     * arg is meatdata or not.
     *
     * @param {*} arg
     * @returns {boolean}
     * @memberof CheckExpress
     */
    isMetadata?(arg: any): boolean;
    /**
     * set arg to metadata.
     *
     * @param {T} metadata
     * @param {*} arg
     * @memberof CheckExpress
     */
    setMetadata(metadata: T, arg: any): void;
}
/**
 * args iterator.s
 *
 * @export
 * @class ArgsIterator
 */
export declare class ArgsIterator {
    protected args: any[];
    private idx;
    private metadata;
    constructor(args: any[]);
    isCompeted(): boolean;
    end(): void;
    next<T>(express: CheckExpress<T>): any;
    getArgs(): any[];
    getMetadata(): Metadate;
    static getClassAnnations(): any;
}
