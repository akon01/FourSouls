import { ObjectMap } from '../types';
import { ProviderMap } from './ProviderMap';
import { ProviderType, ParamProvider } from './Provider';
/**
 * providers.
 * note: ObjectMap<any> provider can not resolve token.
 */
export declare type ProviderTypes = ObjectMap<any> | ProviderMap | ProviderType;
/**
 * params providers.
 */
export declare type ParamProviders = ProviderTypes | ParamProvider;
