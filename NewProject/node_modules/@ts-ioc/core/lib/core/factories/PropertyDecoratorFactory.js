"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var DecoratorFactory_1 = require("./DecoratorFactory");
var DecoratorType_1 = require("./DecoratorType");
var utils_1 = require("../../utils");
/**
 * create property decorator.
 *
 * @export
 * @template T metadata type.
 * @param {string} name decorator name.
 * @param {MetadataAdapter} [adapter]  metadata adapter
 * @param {MetadataExtends<T>} [metadataExtends] add extents for metadata.
 * @returns
 */
function createPropDecorator(name, adapter, metadataExtends) {
    var propPropAdapter = (function (args) {
        if (adapter) {
            adapter(args);
        }
        args.next({
            isMetadata: function (arg) { return utils_1.isProvideMetadata(arg); },
            match: function (arg) { return utils_1.isToken(arg); },
            setMetadata: function (metadata, arg) {
                metadata.provider = arg;
            }
        });
    });
    var decorator = DecoratorFactory_1.createDecorator(name, propPropAdapter, metadataExtends);
    decorator.decoratorType = DecoratorType_1.DecoratorType.Property;
    return decorator;
}
exports.createPropDecorator = createPropDecorator;

//# sourceMappingURL=../../sourcemaps/core/factories/PropertyDecoratorFactory.js.map
